# Copy to .env and fill in. Used by `npm run release` (sourced before npm publish).
# Granular npm access token with publish permission + "bypass 2FA":
# https://www.npmjs.com/settings/guitarstudio/tokens
NPM_TOKEN=npm_your-granular-publish-token-here
