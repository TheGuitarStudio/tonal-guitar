/**
 * Shared GitHub repo identity for the site. Centralized here so the slug
 * doesn't drift if it's duplicated across pages/components — update once if
 * the repo ever moves.
 */
export const REPO_SLUG = "TheGuitarStudio/tonal-guitar";

export const REPO_URL = `https://github.com/${REPO_SLUG}`;
